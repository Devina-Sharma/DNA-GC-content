Purpose
A simple Python script to parse DNA sequences from a FASTA file and calculate their GC content percentage

Output
The script generates a tab-separated file (gc_results.txt) with the following data:
Sequence ID
Sequence Length
GC Content (%)
